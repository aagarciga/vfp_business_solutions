// Generated by CoffeeScript 1.9.3
(function() {
  var $, ARDashboard, App, dandelion, global;

  global = window;

  $ = global.jQuery;

  App = global.App;

  dandelion = global.dandelion;

  ARDashboard = dandelion.namespace('App.ARDashboard', global);

  ARDashboard.status = {};

  ARDashboard.dictionaries = {};

  ARDashboard.htmlBindings = {};

  ARDashboard.functions = {};

  ARDashboard.functions.formatToCurrency = function(value, separator) {
    var offset, result, strValue;
    if (separator == null) {
      separator = ',';
    }
    strValue = value.toString();
    offset = 3;
    if (!(strValue.indexOf('.') === -1)) {
      offset += strValue.slice(strValue.indexOf('.')).length;
    }
    if (strValue.length > 3) {
      result = strValue.slice(0, -1 * offset) + separator + strValue.slice(-1 * offset);
      if (strValue.length > 6) {
        return result.slice(0, -1 * (offset + 4)) + separator + result.slice(-1 * (offset + 4));
      } else {
        return result;
      }
    } else {
      return strValue;
    }
  };

  ARDashboard.eventHandlers = {};

  ARDashboard.eventHandlers.onClickGraph = function(event) {
    return console.log(event);
  };

  ARDashboard.eventHandlers.onHoverGraph = function(event) {
    return console.log(event);
  };

  ARDashboard.init = function() {};

}).call(this);

//# sourceMappingURL=ardashboard.js.map
